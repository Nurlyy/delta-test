1. Дать определение базы данных.
* Совокупность всех данных, используемая только одним пользователем
* Способ представления информации, удовлетворяющий всем требованиям пользователей
* Таблица, в которую внесены все данные о сотрудниках предприятия
* +Совокупность данных, организованных на машинном носителе средствами СУБД
2. MySQL - Это
* База данных «ключ-значение»
* +Реляционная СУБД
* Документо-ориентированная СУБД
* Объектно-ориентированная база данных
3. Для создания новой таблицы в существующей базе данных используют команду:
* TABLE
* NEW TABLE
* +CREATE TABLE
* MAKE TABLE
4. Какой существующий в MySQL тип данных позволяет хранить строковые значения:
* +Char
* String
* BLOCL
* Dec
5. Чем характерна третья нормальная форма по сравнению с 1 и 2?
* +Каждый неключевой атрибут неприводимо зависит от первичного ключа (отсутствие транзитивной зависимости)
* В таблицах есть первичный ключ, однозначно идентифицирующий записи в таблице
* В таблице должен быть только один первичный ключ
* Все используемые множества значений должны содержать только скалярные значения
6. Как расшифровывается SQL?
* +Structured Query language
* Strict Query Language
* Strict Question Line
* Strong Question Language
7. Есть таблица компьютеров PC (model, speed, price). Какой запрос выведет: - все компьютеры, с тактовой частотой больше 1500 МГц; - среднюю стоимость компьютеров с таким же быстродействием?
* SELECT pc.speed, AVG(pc.price) FROM PC HAVING pc.speed > 1500
* SELECT pc.speed, AVG(pc.price) FROM PC WHERE pc.speed > 1500
* SELECT pc.speed, AVG(pc.price) FROM PC ORDER BY pc.speed HAVING pc.speed > 1500
* +SELECT pc.speed, AVG(pc.price) FROM PC WHERE pc.speed > 1500 GROUP BY pc.speed
8. Что возвращает запрос SELECT * FROM Students?
* +Все записи из таблицы «Students»
* Рассчитанное суммарное количество записей в таблице «Students»
* Внутреннюю структуру таблицы «Students»
* Мета информацию о таблице «Students»
9. Выберите верное утверждение:
* SQL чувствителен к регистру в ключевых словах (SELECT, WHERE, IN ...)
* +SQL чувствителен к регистру в названиях таблиц при написании запросов
* SQL чувствителен к регистру в названиях колонок при написании запросов
* SQL нечувствителен к регистру
10. Какой оператор используется для выборки значений в пределах заданного диапазона?
* WITHIN
* IN
* +BETWEEN
* INSIDE
11. В таблице «Emlpoyees» содержатся данные об именах, фамилиях и зарплате сотрудников. Напишите запрос, который изменит значение зарплаты с 2000 на 2500 для сотрудника с ID=7.
* SET Salary=2500 FROM Salary=2000 FOR ID=7 FROM Employees
* ALTER TABLE Employees Salary=2500 FOR ID=7
* ALTER TABLE Employees SET Salary=2500 WHERE ID=7
* +UPDATE Employees SET Salary=2500 WHERE ID=7
12. Напишите запрос, возвращающий информацию о заказчиках, проживающих в одном из городов: Ванкувер, Хельсинки, Берлин.
* SELECT Vancouver, Helsinki, Berlin FROM Customers
* +SELECT * FROM Customers WHERE City IN (‘Vancouver’, ‘Helsinki’, ‘Berlin’)
* SELECT City IN (‘Vancouver’, ‘Helsinki’, ‘Berlin’) FROM Customers
* SELECT City FROM (‘Vancouver’, ‘Helsinki’, ‘Berlin’) WHERE Customers
13. Выберите самый безопасный способ подстановки переменных в SQL
* Конкатенация “WHERE ID = ” + val
* f строки f”WHERE ID = {val}”
* Использование метода format для строки ”WHERE ID = {0}”.format(val)
* +Плейсхолдеры %s “WHERE ID = %s”
14. Для чего нужен объект курсора в модуле pymysql
* Установить соединение с базой данных MySQL.
* +Для выполнения SQL-запросов и извлечения результатов из базы данных.
* Для управления транзакциями.
* Для обработки ошибок и исключений, которые могут возникнуть во время взаимодействия с базой данных.
15. Выберите правильные варианты ответа для чего нужно закрывать соединение с базой, когда оно больше не нужно.
* +Чтобы освободить системные ресурсы и предотвратить их утечку. 
* Для обеспечения целостности и непротиворечивости данных. 
* Для повышения производительности и оптимизации использования ресурсов. 
* +Чтобы предотвратить возможно исчерпания лимита на максимальное количество открытых соединений.
* Для предотвращения несанкционированного доступа и потенциальных нарушений безопасности.
16. Укажите операторы фильтрации выборок в MySQL
* INSERT
* +WHERE
* +HAVING
* FILTER
17. Есть таблица с полями name и id. Какой запрос выводит имя с наибольшим id без использования команды MAX.
* SELECT DISTINCT name, id FROM customers
* SELECT name, id FROM customers GROUP BY id
* SELECT name, id FROM customers GROUP BY id LIMIT 1
* +SELECT name, id FROM customers ORDER BY id DESC LIMIT 1
18. Дана таблица Cars: Каким будет результат следующего запроса: SELECT COUNT (DISTINCT color) FROM cars
* red blue null
* 3
* 2 1 1
* +2
19. Есть библиотека с книгами. Оперируя сущностями «Книга» и «Автор», Вы хотите сохранить реестр своей библиотеки в БД. Сколько таблиц понадобится для эффективного хранения такой структуры в 3-й нормальной форме?
* 2
* +3
* 4
* 1
20. Для однозначной идентификации записей в базе данных служит…
* +Первичный ключ
* Индекс
* Атрибут
* Домен отношения
21. Для первичного ключа ложно утверждение, что …
* +Первичный ключ может принимать нулевое значение
* В таблице может быть назначен только один первичный ключ
* Первичный ключ может быть простым и составным
* Первичный ключ однозначно определяет каждую запись в таблице
22. Для чего нужны индексы базы данных? 
* Для хранения больших объемов данных 
* Чтобы сделать резервную копию базы данных 
* +Для повышения производительности операций поиска данных 
* Для шифрования конфиденциальных данных 
23. Какая структура данных обычно используется для реализации индексов базы данных? 
* Связанные списки 
* Стеки 
* Хэш-таблицы 
* +B-деревья
24. Какой результат будет получен при выполнении INNER JOIN между таблицами "Customers" и "Orders" по столбцу "customer_id"? 
* Все записи из таблицы "Customers" и соответствующие записи из таблицы "Orders". 
* Все записи из таблицы "Customers" и все записи из таблицы "Orders". 
* +Только записи из таблицы "Customers", которые имеют соответствующие записи в таблице "Orders". 
* Только записи из таблицы "Orders", которые имеют соответствующие записи в таблице "Customers".
25. Какой SQL запрос правильно соответствует следующей ситуации: <code>У вас есть две таблицы, "Customers" и "Orders". Таблица "Customers" содержит столбец "customer_id", а таблица "Orders" содержит столбец "customer_id". Вы хотите получить только те записи из таблицы "Customers", для которых есть соответствующие записи в таблице "Orders".</code>
* +SELECT * FROM Customers INNER JOIN Orders ON Customers.customer_id = Orders.customer_id; 
* SELECT * FROM Customers JOIN Orders ON Customers.customer_id = Orders.customer_id; 
* SELECT * FROM Customers LEFT JOIN Orders ON Customers.customer_id = Orders.customer_id; 
* SELECT * FROM Customers RIGHT JOIN Orders ON Customers.customer_id = Orders.customer_id;